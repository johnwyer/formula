import React from 'react';
import UserLayout from '../../hoc/user';

const UserDashboard = () => {
    return (
        <UserLayout>
            User dashboard
        </UserLayout>
    );
};

export default UserDashboard;